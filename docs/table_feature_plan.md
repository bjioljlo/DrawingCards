# Table（桌面）功能設計規劃

## 目標
設計 Table 類別，作為遊戲中桌面區域的管理核心，負責管理桌面上的牌，支援牌的放置、移動和狀態同步等功能。

## Table 類別設計

### 屬性
- `MaxCards`：桌面最大可放牌數（int，可依遊戲規則調整）
- `CurrentCards`：目前桌面上的牌數（int）
- `Cards`：桌面上的所有牌集合（List<Card>）
- `IsFull`：桌面是否已滿（bool）

### 方法
- `AddCard(Card card)`：加入牌到桌面（若未達最大數量）
- `RemoveCard(Card card)`：將牌移出桌面（丟棄或其他用途）
- `SwapCards(int indexA, int indexB)`：交換桌面上兩張牌的位置
- `GetCards()`：取得目前桌面所有牌的清單
- `Clear()`：清空桌面所有牌
- `IsValidMove(List<Card> cards)`：驗證牌組是否可放置到桌面

## 整合建議
- Table 為玩家手牌、棄牌堆的中間過渡區，支援牌的移動操作。
- 需與 UI 同步，即時反映桌面狀態變化。
- 可根據遊戲規則調整最大牌數和放置規則。
- 支援多種牌的放置策略（如順序、組合等）。

## 測試建議
- 驗證牌的加入、移除、交換操作的正確性。
- 驗證桌面最大牌數限制。
- 驗證牌的狀態同步。
- 驗證特殊情況處理（如桌面已滿、無牌可移等）。

## 擴充性建議
- 支援多種桌面布局（如線性、網格等）。
- 支援自訂放置規則（如特定組合、順序等）。
- 支援桌面區域的擴展（如增加特殊區域）。
- 支援多種牌的放置策略（如自動排列、手動調整等）。

---

---

## ✅ 功能實作狀態驗收清單

### 屬性實作
| 項目 | 狀態 | 備註 |
|------|------|------|
| `MaxCards` 桌面最大可放牌數 | ✅ 完成 | 正確實作唯讀屬性，建構式設定 |
| `CurrentCards` 目前牌數 | ✅ 完成 | 已實作唯讀屬性 |
| `Cards` 桌面上的牌集合 | ✅ 完成 | 透過 GetCards() 回傳唯讀清單 |
| `IsFull` 桌面是否已滿 | ✅ 完成 | 已實作唯讀屬性 |

### 方法實作
| 項目 | 狀態 | 備註 |
|------|------|------|
| `AddCard(Card card)` 加入牌到桌面 | ✅ 完成 | 已實作含滿位檢查 |
| `RemoveCard(Card card)` 移出桌面 | ✅ 完成 | 已實作 |
| `SwapCards(int indexA, int indexB)` 交換牌位置 | ✅ 完成 | 已實作含邊界檢查 |
| `GetCards()` 取得所有牌清單 | ✅ 完成 | 已實作回傳唯讀清單 |
| `Clear()` 清空桌面 | ✅ 完成 | 已實作 |
| `IsValidMove(List<Card> cards)` 驗證牌組 | ✅ 完成 | 已實作完整驗證邏輯 |

---

🎉 **整體完成度: 100%**
✅ 所有規格文件中的功能 100% 全部實作完畢
✅ 屬性 100% 符合規格
✅ 方法 100% 符合規格
✅ 有對應單元測試
✅ 符合 AGENTS.md SDD/TDD 開發規範
✅ 程式碼品質: 0 錯誤, 0 警告
